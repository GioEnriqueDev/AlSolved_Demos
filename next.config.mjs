/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/AlSolved_Demos',
    assetPrefix: '/AlSolved_Demos/',
    images: {
        unoptimized: true,
    },
};

export default nextConfig;
